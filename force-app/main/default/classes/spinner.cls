public with sharing class Spinner {
    public static String consultaJson(String cpf){

    }
     public static cpf parse(String json) {
        return System.JSON.deserialize(json, teste.JSON);
    }
}
